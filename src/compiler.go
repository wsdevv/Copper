package main
/*
 * FILE: compiler.go
 * PURPOSE: Holds the functions to inteperate and compile code, uses lexer functions
 */

import (
	"fmt"
	
)

/* being rewritten */
func compile(program string, file string) {

	// creates a new instance of the Lexer object (lexer.go)
	lexer := new_lexer(program);

	
	
	
}
